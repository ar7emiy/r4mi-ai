{
  "name": "r4mi-ai-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "PWSLOWMO=400 playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:health": "playwright test health",
    "test:demo": "playwright test demo",
    "test:beat": "playwright test --grep"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
